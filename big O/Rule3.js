//Different terms for inputs

function compressBoxesTwice(boxes, boxes2) { // two different inputs

    boxes.forEach(function(boxes) {
        console.log(boxes);
    });

    boxes2.forEach(function(boxes) {
        console.log(boxes);
    });
}

// O(a + b)